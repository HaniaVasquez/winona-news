import { makeContentfulClient, mapPost } from '../../utils/contentful'export default defineEventHandler(async event => {  const slug = event.context.params?.slug as string  const preview = getQuery(event).preview === 'true'  const client = makeContentfulClient(preview)  const res = await client.getEntries({    content_type: 'blogPost',    'fields.slug': slug,    limit: 1,  })  if (!res.items.length) {    throw createError({ statusCode: 404, statusMessage: 'Post not found' })  }  return mapPost(res.items[0])})