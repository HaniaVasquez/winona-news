import { createClient } from 'contentful'import type { ContentfulRawPost, ContentfulPost } from '~/types/contentful'export default defineEventHandler(async () => {  try {    const config = useRuntimeConfig()    const client = createClient({      space: config.CONTENTFUL_SPACE_ID!,      accessToken: config.CONTENTFUL_DELIVERY_TOKEN!,      environment: config.CONTENTFUL_ENVIRONMENT || 'master',    })    const entries = await client.getEntries<ContentfulRawPost>({      content_type: 'pageBlogPost',      order: ['-fields.publishedDate'],    })    return entries.items.map(entry => ({      id: entry.sys.id,      title: entry.fields.title,      slug: entry.fields.slug,      description: entry.fields.description,      image: entry.fields.articleImage,      date: entry.fields.publishedDate,      content: entry.fields.content,    }))  } catch (error) {    console.error('Error fetching posts:', error)    throw createError({      statusCode: 500,      statusMessage: 'Failed to fetch posts from Contentful',    })  }})